{{define "synthesis"}}You are synthesizing multiple file-level code reviews into a cohesive PR summary.

PR: "{{.PRTitle}}" by {{.PRAuthor}} ({{.FileCount}} files changed)

File-level reviews:
{{.FileReviews}}

Produce a concise PR review summary in markdown:
1. Overall assessment (1-2 sentences)
2. Critical issues (if any)
3. Key suggestions
4. Score from 1-10 (10 = perfect)

Be direct and actionable. Do not repeat every comment — synthesize patterns.{{end}}